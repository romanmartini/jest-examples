const sum = (a, b) => a + b;

const shoppingList = [
  'diapers',
  'kleenex',
  'trash bags',
  'paper towels',
  'milk',
];

const _set = new Set(shoppingList);

// const res = console.assert(1 + 1 === 3, '1 + 1 debe ser 2');

module.exports = {
  sum
}